# Placeholder for this step only — proves docker-compose wiring works.
# Real Node/TypeScript app (package.json, src/) gets added in a later step.
FROM node:20-slim
CMD ["node", "-e", "console.log('backend placeholder up'); setInterval(() => {}, 1000 * 60 * 60)"]